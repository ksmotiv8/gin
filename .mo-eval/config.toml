language = "go"
test_command = "go test"
# This checkout is a fork; merged pull requests are the record of the UPSTREAM repository.
# The forge's PR list comes from here, file content from the local clone (README → "only on a fork").
repo = "gin-gonic/gin"
# gin's go.mod requires go 1.26.0; a 1.24 image refuses the build (GOTOOLCHAIN=local).
worker_image = "golang:1.26-bookworm@sha256:a688600ca24f8a4d3ca77f95b0dd40704a9fc787c826660eb7ba0b641b8b175d"
